UNDP Human Development Report composite indices (latest recalculated series).

- License: CC BY 3.0 IGO
- Join: `iso3`. Never country names.
- HDI hosted here is the current HDR recalculated time series. Ranks are not back-comparable to older printed reports.

Refresh: `pnpm data:fetch` then `pnpm data:normalize` then `pnpm data:validate`.
