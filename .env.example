# Canonical site URL used at build for sitemap, Open Graph, and canonical links.
# Cloudflare Pages preview builds already receive CF_PAGES_URL.
SITE=https://geo-index-8gl.pages.dev

# Optional: wrangler deploy from GitHub Actions (not required if Cloudflare
# is connected to GitHub and pulls the production branch).
# CLOUDFLARE_ACCOUNT_ID=
# CLOUDFLARE_API_TOKEN=
