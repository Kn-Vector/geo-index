# Geo Index — Cloudflare Pages project (static atlas).
# Production branch is `production`. Other branches and PRs are preview.
name = "geo-index"
compatibility_date = "2026-09-03"
pages_build_output_dir = "apps/web/dist"

[vars]
SITE = "https://geo-index-8gl.pages.dev"
