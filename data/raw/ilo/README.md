ILOSTAT modelled-estimates bulk CSV (plumber indicator tables).

- License: CC BY 4.0
- Join: `ref_area` = ISO alpha-3
- Filters: SEX_T and AGE_YTHADULT_YGE15 (15–24 for youth unemployment)

Refresh: `pnpm data:fetch` then `pnpm data:normalize` then `pnpm data:validate`.
