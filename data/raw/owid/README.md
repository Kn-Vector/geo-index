Our World in Data CO2 dataset, citing the Global Carbon Project as the original source.

- Not IEA-EDGAR (CC BY-NC-ND).
- OWID compilation: CC BY 4.0. Join on `iso_code`. Skip rows without ISO3.

Refresh: `pnpm data:fetch` then `pnpm data:normalize` then `pnpm data:validate`.
